name: driver_app
description: "Iniato Driver — route-based shared mobility driver app."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  mapbox_maps_flutter: ^2.3.0
  http: ^1.2.2
  shared_preferences: ^2.2.0
  geolocator: ^13.0.2
  permission_handler: ^11.0.0
  stomp_dart_client: ^1.0.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
